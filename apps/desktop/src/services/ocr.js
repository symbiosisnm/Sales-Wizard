/**
 * OCR service for Cheating Daddy.
 * Accepts a base64-encoded PNG and returns recognized text.
 * Currently returns an empty string (placeholder).
 * TODO: Implement actual OCR using tesseract.js or a cloud OCR service.
 */
export async function ocrBase64(base64) {
  // Placeholder implementation: returns empty string
  return '';
}
